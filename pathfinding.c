#include <stdio.h>

const int NORTH = map[-1][0];
const int SOUTH = map[1][0];
const int EAST = map[0][1];
const int WEST = map[0][-1];

int *short_way(int a[][], int b[][])
{
  int queue[]=calloc(400*(sizeof(int)));
  int insert = 1;
  int scroll = 0;
  queue[0] = a;
  while (insert > scroll)
  {
    if
  }
}
